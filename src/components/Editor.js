import Paper from '@material-ui/core/Paper'
import { useStyles } from '../utils/styles'

export const Editor = () => {
 const classes = useStyles()
  return <Paper className={classes.paper}>Editor</Paper>
}
